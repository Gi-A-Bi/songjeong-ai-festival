# Claude Code 복사용 프롬프트: 실시간 대시보드·학급 최종 미션 개편

아래 코드 블록 전체를 Claude Code에 붙여 넣는다.

```text
이 프로젝트는 서울송정초등학교의 송정 AI 페스티벌 웹앱이다.

작업을 시작하기 전에 다음 문서를 처음부터 끝까지 읽어라.

1. CLAUDE.md
2. AI_FESTIVAL_SPEC.md
3. CARD_FINALE_UPDATE_SPEC.md

CARD_FINALE_UPDATE_SPEC.md 버전 3.0이 실시간 운영 대시보드, QR 체크인, 카드 힌트, 학급 최종 미션, 최종 순위에 관한 가장 최신 요구사항이다. 기존 문서나 코드가 이 문서와 충돌하면 버전 3.0을 우선한다.

특히 다음 구버전 규칙은 폐기되었다.

- 팀별로 서로 다른 결승 문제를 푸는 구조
- 다섯 팀의 답을 조합한 최종 코드
- 카드 종류마다 다른 찬스
- AI 마스터 찬스
- 같은 학년의 강제 동시 출발
- 첫 정답 코드 제출 시각만으로 순위를 정하는 방식

먼저 프로젝트의 실제 폴더 구조, package.json, 라우트, 현재 데이터 모델, repository 구현, 인증 역할, 카드와 결승 관련 컴포넌트 및 테스트를 확인하라. 내가 만든 파일이나 미커밋 변경을 보존하고, 파괴적인 Git 명령을 실행하지 마라.

구현 전에 다음을 간결하게 보고하라.

1. 현재 구현되어 있는 기능과 아직 목업인 기능
2. 최신 명세와 충돌하여 제거하거나 바꿔야 할 코드
3. 수정할 주요 파일과 새로 만들 주요 파일
4. 이번 작업을 나눌 작은 구현 단계와 각 단계의 완료 조건

한 번에 앱 전체를 재작성하지 마라. 아래 순서로 작은 수직 기능 단위를 구현하고 각 단계가 끝날 때 관련 테스트를 실행하라.

## 1단계: 최신 도메인 모델과 구버전 제거

- CARD_FINALE_UPDATE_SPEC.md의 최신 타입을 현재 프로젝트 구조에 맞게 반영한다.
- EventRepository 인터페이스를 먼저 확장하고 MockEventRepository를 구현한다.
- UI 컴포넌트에서 Firestore SDK나 목업 데이터를 직접 호출하지 않는다.
- 카드 종류별 찬스, AI 마스터 찬스, 팀별 결승 답, 최종 코드, 30초 재제출 잠금에만 사용되는 코드·타입·문구·테스트를 제거하거나 마이그레이션한다.
- 기존 `/draw`, `/exchange`, 팀별 결승 경로는 새 카드 현황 또는 적절한 안내 화면으로 리다이렉트한다.
- 아직 Firebase 단계가 아니라면 Firebase 연결을 억지로 추가하지 말고 mock 모드에서 전체 동선을 완성한다.

## 2단계: QR 체크인과 팀 이동 상태

- 팀 번호와 라운드로 예상 미션을 계산한다.
- 팀 화면에서 미션 교실 QR을 읽거나 QR URL로 들어왔을 때 체크인한다.
- 상태 흐름은 `scheduled → checked_in → active → completed → moving`이다.
- 예정과 다른 미션이면 `wrong_station` 경고를 만들고 올바른 교실을 안내한다.
- 라운드 시작 후 기본 2분이 지나도 체크인하지 않으면 `not_arrived` 경고를 계산한다.
- 라운드 종료 뒤 결과가 없으면 `result_missing`으로 표시한다.
- 동일 QR 재스캔과 제출 버튼 연타는 같은 결과를 다시 만들지 않게 한다.

## 3단계: 실시간 운영 대시보드

`/teacher/:eventId/dashboard`에 현재 학년의 전체 진행을 보여주는 대시보드를 구현한다.

필수 영역:

- 학년 선택, 현재 라운드, 활동·이동 타이머, 입장·완료 팀 수, 경고 수
- 5개 미션 교실별 예정 팀과 실제 상태
- 학급 행 × 1~5팀 열의 현재 미션·상태 표
- 학급 선택 시 완료 미션, 순위, 카드 진행도, 완성 카드 수 상세
- 중요한 상태 변경만 표시하는 최근 활동 기록
- 미도착, 잘못된 교실, 결과 미입력을 모아 보여주는 확인 필요 영역

부스 교사 화면에는 예정 팀 확인, `미션 시작`, 순위·결과 입력, `결과 확정`의 핵심 동작만 둔다. 결과 확정은 한 번만 카드 보상을 생성해야 한다.

역할별 수정 권한을 제한한다.

- admin: 전체 수정과 복구
- station_teacher: 담당 미션만 수정
- homeroom_teacher: 담당 학급 최종 미션만 시작·진행

전체 현황은 교사에게 읽기 전용으로 보여줄 수 있다.

## 4단계: 카드 성장과 공통 힌트 수 계산

카드 종류는 `thinking`, `observation`, `expression`, `command`, `verification` 다섯 개다.

순위별 카드 선택권은 유지한다.

- 1위: 서로 다른 3종 중 1종 선택
- 2위: 서로 다른 2종 중 1종 선택
- 3위 이하: 1종 무작위 자동 배정

카드는 학급 단위로 같은 종류 4조각을 모으면 완성된다. 기존 카드 이미지를 CSS 마스크 또는 오버레이로 네 영역에 나누어 공개하고 조각 이미지를 새로 만들지 마라. 5번째 이후는 `+N`만 기록한다.

최신 최종 미션 규칙:

- 완성 카드 종류 수 = 사용 가능한 힌트 수
- 중복 조각은 힌트 수를 늘리지 않음
- 카드 종류와 무관하게 모든 힌트의 효과가 같음
- 5종 완성 여부는 최종 순위의 두 번째 판정 기준
- AI 마스터 찬스 없음

## 5단계: 학급별 최종 미션 시작

총괄 운영자가 5라운드와 결과 입력을 확인한 뒤 학년의 최종 미션을 연다. 그 전에는 담임교사의 시작 버튼을 비활성화한다.

각 반은 준비되었을 때 따로 시작한다.

- 시작 확인 대화상자
- `3, 2, 1, 시작!` 카운트다운
- repository의 서버 시각으로 시작을 한 번만 기록
- 새로고침해도 같은 시작 시각과 진행 상태 복구
- 시작 시 완성 카드 종류 수, 5종 완성 여부, 힌트 총수를 스냅샷으로 고정
- 관리자만 사유를 입력해 초기화 또는 수동 보정 가능

반마다 시작 시각은 달라도 되며 순위에는 절대 시각이 아니라 실제 소요 시간을 사용한다.

## 6단계: 전자칠판용 10문제 최종 미션

`/teacher/:eventId/class/:classId/final`에 학급 전체가 전자칠판으로 함께 푸는 화면을 구현한다.

- 10문제
- 모두 4지선다형
- 기본 제한 시간 12분, 설정으로 변경 가능
- 같은 학년에는 같은 문제와 제한 시간
- 생각·관찰·표현·명령·검증 영역을 기본 2문제씩 구성
- 문제, 보기, 정답, 제거할 오답을 컴포넌트에 하드코딩하지 않고 repository 또는 config에서 제공
- 현재 문제 번호, 시간, 남은 힌트, 보기, 힌트 버튼, 답 확정 버튼 표시
- 답 확정 전에는 변경 가능, 확정 후 다음 문제로 이동
- 중간에는 정답 여부 비공개
- 답과 현재 문제를 저장해 새로고침 후 복구
- 마지막 문제 뒤 전체 제출
- 제한 시간 종료 시 저장된 답안을 마감하고 미응답은 오답 처리
- 자동 제출이 실패하면 총괄 대시보드에 검토 필요 상태 표시

힌트 규칙:

- 완성 카드 종류 수만큼 사용 가능, 최대 5개
- 힌트 1개를 사용하면 현재 문제의 틀린 보기 1개 제거
- 한 문제에 최대 1개
- 정답 보기는 절대 제거하지 않음
- 사용 기록을 저장해 새로고침이나 연타로 두 번 차감되지 않게 함

## 7단계: 집계와 순위

최종 제출 때 다음 값을 계산한다.

```text
correctCount = 맞힌 문제 수
durationMs = submittedAt - startedAt
```

최종 순위 정렬은 정확히 다음 순서를 사용한다.

```text
correctCount DESC
→ allFiveCardsCompletedSnapshot DESC
→ durationMs ASC
```

- 정답 수가 가장 중요하다.
- 정답 수가 같으면 5종 카드를 모두 완성한 학급이 우선한다.
- 카드 완성 여부도 같으면 소요 시간이 짧은 학급이 우선한다.
- 둘 다 5종 미완성인 동점 학급도 시간으로 판정한다.
- 세 값이 모두 같으면 공동 순위다.
- 완성 카드가 3종인지 4종인지는 추가 동점 기준으로 쓰지 않는다.

진행 중에는 일반 학급 화면에 다른 반의 점수와 순위를 공개하지 않는다. 상태, 현재 문제 번호, 경과 시간, 남은 힌트만 실시간 중계한다. 같은 학년의 모든 반이 제출한 뒤 총괄 운영자가 결과를 공개한다.

## 8단계: 무료 사용량과 안정성

- 타이머를 매초 Firestore에 쓰지 말고 저장된 시작 시각으로 화면에서 계산한다.
- 대시보드 실시간 구독은 현재 학년과 현재 라운드의 작은 상태 문서로 제한한다.
- 큰 이미지 제출 문서를 실시간 구독하지 않는다.
- QR 체크인, 결과 확정, 카드 수령, 힌트 사용, 최종 시작·제출을 멱등 처리한다.
- Firebase Cloud Functions, Cloud Storage, Firebase AI Logic, 외부 생성형 AI API, 결제 수단이 필요한 서비스를 추가하지 않는다.
- 학생 이름, 학번, 이메일을 수집하지 않는다.

## 필수 테스트

CARD_FINALE_UPDATE_SPEC.md 15절의 테스트를 빠짐없이 반영한다. 최소한 다음을 포함한다.

- 예상 미션 계산
- 정상·오입장·중복 QR 체크인
- 부스 시작과 결과 확정 상태 전환
- 카드 보상 중복 방지
- 대시보드 요약과 경고 계산
- 카드 0/4~4/4, 중복 +N, 완성 종류 수
- 힌트 총수 스냅샷
- 학급별 시작 멱등성
- 10문제 진행과 새로고침 복구
- 힌트가 오답만 한 번 제거하는지 확인
- 제한 시간 마감
- 서버 시각 기반 소요 시간
- `정답 수 → 5종 완성 → 시간` 순위 정렬
- 결과 공개 전 점수·순위 숨김
- 관리자 보정 사유 검증

## 문서 동기화

구현과 함께 `AI_FESTIVAL_SPEC.md`, `CLAUDE.md`, 샘플 데이터와 README의 관련 문구를 최신 규칙으로 수정하라. 다음 구버전 표현이 제품 요구사항으로 남아 있는지 전체 검색하고 제거하라.

- 카드별 찬스
- AI 마스터 찬스
- 팀별 결승 문제
- 최종 코드
- 동시 결승 시작
- 첫 정답 제출 반 우승
- 30초 재제출 잠금

사용하지 않는 `master-chance-emblem`과 구버전 장면 이미지는 즉시 삭제하지 않아도 되지만 신규 화면에서 참조하지 않는다.

## 완료 검사

프로젝트에 존재하는 명령을 확인한 뒤 가능한 검사를 모두 실행하라.

- npm run lint
- npm run typecheck
- npm run test -- --run
- npm run build

명령이 없다면 package.json에 실제 존재하는 대체 명령을 사용한다. 오류가 나면 원인을 수정하고 같은 검사를 다시 실행한다. 실행하지 못한 검사를 성공했다고 가정하지 마라.

마지막 보고는 다음 네 항목만 간결하게 작성하라.

1. 구현한 내용
2. 변경한 주요 파일
3. 실행한 검사와 결과
4. 다음 한 단계
```
