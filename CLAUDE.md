# CLAUDE.md

이 저장소는 서울송정초등학교의 “송정 AI 페스티벌” 운영 웹앱이다. 작업을 시작하기 전에 `AI_FESTIVAL_SPEC.md`와 `CARD_FINALE_UPDATE_SPEC.md` 전체를 읽는다. 실시간 운영 대시보드, QR 체크인, 카드 성장, 공통 힌트, 학급 최종 미션, 최종 순위에 관한 충돌은 `CARD_FINALE_UPDATE_SPEC.md` 버전 3.0을 최신 제품 요구사항으로 사용한다.

## 프로젝트 목표

- 3~6학년 학생들이 5개 미션 교실을 팀별로 순환한다.
- 한 학년 약 100명, 팀 기기 20~30대가 동시에 사용한다.
- 학생은 팀 QR로 입장하고 미션 교실 QR로 도착을 기록하며, 미션 결과에 따른 카드 종류를 선택한다.
- 교사는 대시보드에서 모든 팀의 위치와 상태, 채점, 카드 성장, 학급별 최종 미션을 실시간으로 관리한다.
- 최종 미션은 반별로 준비되었을 때 시작하고 학급 전체가 전자칠판으로 10문제를 함께 푼다.
- 학생 개인 정보와 유료 AI API 없이 운영한다.

## 가장 중요한 원칙

1. 사용자가 명시적으로 승인하지 않은 유료 서비스나 결제 필요 기능을 추가하지 않는다.
2. MVP는 Firebase Spark 플랜에서 동작해야 한다.
3. Cloud Storage, Cloud Functions, Firebase App Hosting, 전화번호 인증, Firebase AI Logic을 사용하지 않는다.
4. 프로그램 내부에서 생성형 AI를 호출하거나 그림을 자동 채점하지 않는다.
5. 학생 이름, 학번, 이메일을 수집하지 않는다.
6. UI는 한국어이며 초등학생이 3초 안에 다음 행동을 알 수 있어야 한다.
7. 교사는 모든 자동 결과를 확인하고 수동으로 수정할 수 있어야 한다.
8. 새 기능보다 행사 당일의 안정성과 복구 가능성을 우선한다.

## 기술 기준

- React + TypeScript + Vite
- React Router
- Firebase Authentication + Cloud Firestore + Firebase Hosting
- Vitest + Testing Library
- ESLint + Prettier
- 기본 스타일은 CSS 변수와 작은 재사용 컴포넌트
- 그림판은 HTML Canvas API
- 일반 아이콘은 Material Symbols Rounded

정확한 패키지 버전은 설치 시점의 안정 버전을 사용하되, 불필요한 의존성을 추가하지 않는다.

## 개발 순서

현재 단계가 명시되지 않았다면 가장 앞의 미완료 단계만 작업한다.

1. 로컬 목업과 전체 화면 동선
2. Firebase 인증·데이터·보안 규칙
3. 골든벨과 틀린그림 찾기
4. 그리기, 오조봇, 도서관 오류찾기
5. 실시간 대시보드, 네 조각 카드 성장, 학급 전체 최종 미션
6. 리허설, 내보내기, 배포

한 번의 요청에서 전체 앱을 재작성하지 않는다. 작은 수직 기능 단위로 구현하고 검증한다.

## 작업 방식

1. 요청과 관련된 기존 파일을 먼저 읽는다.
2. 구현 전에 변경할 파일과 완료 조건을 짧게 제시한다.
3. 기존 사용자 변경을 보존한다.
4. 가장 작은 동작 단위로 구현한다.
5. lint, typecheck, test, build 중 프로젝트에 있는 검사를 실행한다.
6. 오류가 나면 원인을 고친 뒤 같은 검사를 다시 실행한다.
7. 마지막에 변경 파일, 검증 결과, 남은 작업을 짧게 보고한다.

파괴적인 Git 명령을 실행하지 않는다. 비밀키, 서비스 계정 파일, 실제 Firebase 설정값을 커밋하지 않는다.

## 데이터 계층 규칙

화면 컴포넌트에서 Firestore SDK를 직접 호출하지 않는다.

- EventRepository 인터페이스를 경계로 사용한다.
- 1단계는 MockEventRepository로 완성한다.
- 2단계부터 FirestoreEventRepository를 추가한다.
- 환경 변수 VITE_DATA_MODE로 mock과 firebase를 전환한다.
- 서버 시각이 필요한 값은 Firebase server timestamp를 사용한다.
- 쓰기 작업은 고정 문서 ID 또는 requestId로 멱등성을 갖게 한다.
- 화면에서 시각이 기준인 계산은 `Date.now()` 대신 저장소의 `serverNow()`(화면 훅은 `useServerNow`)를 쓴다.
- 아직 Firestore에 연결하지 않은 기능은 저장소 `capabilities`로 꺼 두고, 화면은 그 값을 보고 메뉴와 버튼을 숨긴다.
- 최종 미션의 정답과 힌트 제거 대상은 화면 컴포넌트로 내려 주지 않는다.

원본 데이터:

- 순위: results
- 카드 획득: claimed cardAwards
- 팀 이동: teamMissionStates(체크인), missionRoundStates(미션 시작·결과 확정 팀)
- 최종 미션: finalSessions, finalResponses, finalClassStates, finalQuestionSets, finalAnswerKeys(총괄만 읽음)

카드 진행도 캐시는 편의를 위한 값일 뿐이며 cardAwards 원장과 충돌하면 원장을 우선한다.

## 학생 세션

- 학생은 Firebase 익명 인증을 사용한다.
- QR 링크의 eventId와 teamId로 입장한다.
- 첫 입장 UID를 한 팀에 잠근다.
- 학생은 자기 팀 제출만 생성하거나 수정할 수 있다.
- 학생이 순위, 보상 후보, 카드 원장, 최종 미션 답안을 바꾸게 해서는 안 된다.
- 학생 인증은 학교 행사 수준이며 민감정보를 저장하지 않는다.

## 교사 권한

- 교사는 Google 로그인 사용
- teachers/{uid}에 등록되고 active가 true인 사용자만 허용
- role 값은 admin, station_teacher, homeroom_teacher 중 하나
- 교사 등록을 위한 비밀 PIN을 프런트엔드에 넣지 않는다.
- 보안 규칙 테스트 없이 Firebase 단계를 완료했다고 말하지 않는다.

## 그림 제출

- Firebase Storage를 사용하지 않는다.
- Canvas를 최대 960×540 WebP로 내보낸다.
- 품질 0.65에서 시작해 300KB 이하가 되도록 단계적으로 압축한다.
- 350KB를 넘으면 저장하지 않고 사용자에게 안내한다.
- 그림 데이터는 별도 drawingSubmissions 문서에 bytes로 저장한다.
- 이미지 필드는 Firestore 인덱스에서 제외한다.
- 교사 화면은 이미지 문서를 실시간 구독하지 않는다.
- 교사가 일괄 다운로드한 후 관리자에게 그림 데이터 삭제 기능을 제공한다.

## 무료 사용량 보호

- 대시보드 쿼리는 현재 학년과 라운드로 제한한다.
- 큰 컬렉션을 무제한 실시간 구독하지 않는다.
- 이미지 문서를 목록 조회에 섞지 않는다.
- 폴링보다 작은 상태 문서의 실시간 구독을 사용한다.
- 구독 결과는 저장소 안 메모리 캐시에 두고 화면은 그 캐시에서 만든다. "변화 알림 → 목록 전체 다시 읽기" 방식은 읽기가 교사 수만큼 곱해지므로 쓰지 않는다.
- 개발용 자동 새로고침이나 반복 쿼리가 Firestore 읽기를 폭증시키지 않게 한다.
- 비용이 발생할 수 있는 변경은 구현 전에 사용자에게 명확히 알리고 승인을 기다린다.

## 디자인 시스템

학생용:

- 태블릿 가로 화면 우선
- 본문 18px 이상
- 주요 버튼 높이 56px 이상
- 터치 영역 최소 48×48px
- 한 화면에 핵심 행동 하나
- 성공·실패·대기는 색상뿐 아니라 아이콘과 문구로 표시
- 애니메이션 감소 환경설정 존중

교사용:

- 정보 밀도는 높일 수 있지만 버튼과 상태명은 명확히 유지
- 위험 동작은 확인 대화상자 사용
- 모든 자동 순위는 수동 수정 가능

색상과 간격은 src/styles/tokens.css의 CSS 변수로 관리한다. 임의의 색상값과 간격값을 여러 컴포넌트에 반복하지 않는다.

## 자산 사용

이미지는 public/assets/festival에 있고 학교 로고는 public/assets/brand/school-logo.png에 있다.

- hero-main.webp: 시작 화면
- mission-*.webp: 미션 카드와 안내
- card-*.webp: 네 조각 카드 성장과 카드 현황
- master-chance-emblem.webp: 구버전 자산이며 신규 화면에서 사용하지 않음
- scene-*.webp: 결승 진입과 피날레
- mascot-*.webp: 피드백과 상태

이미지 경로를 컴포넌트마다 직접 반복하지 말고 asset manifest 모듈에서 관리한다. 의미 있는 이미지는 한국어 대체 텍스트를 제공하고, 장식 이미지는 빈 대체 텍스트를 사용한다.

## 미션 공통 인터페이스

각 미션은 공통 셸 안에서 다음 정보를 제공한다.

- 미션 ID와 유형
- 팀과 라운드
- 상태: waiting, active, submitted, scoring, closed
- 종료 시각
- 제출 가능 여부
- 교사 판정 여부

미션마다 타이머와 연결 상태 UI를 새로 만들지 말고 공통 컴포넌트를 재사용한다.

## 오류 처리

- 네트워크 오류가 나도 입력 내용을 지우지 않는다.
- 제출 버튼 연타를 방지한다.
- 재시도는 같은 제출을 중복 생성하지 않는다.
- 사용자 화면에는 이해하기 쉬운 한국어 오류를 표시한다.
- 개발 로그에는 개인정보나 이미지 바이트를 출력하지 않는다.
- 교사에게 수동 복구 경로를 제공한다.

## 완료 정의

기능은 다음을 만족해야 완료다.

- 타입 오류 없음
- lint 통과
- 관련 테스트 통과
- production build 통과
- 태블릿 가로 및 세로 화면에서 주요 기능 확인
- 빈 상태, 로딩, 성공, 실패 상태 구현
- 접근성 이름과 포커스 확인
- AI_FESTIVAL_SPEC.md의 관련 인수 조건 확인

검사를 실행할 수 없으면 성공했다고 가정하지 말고, 실행하지 못한 이유와 사용자가 실행할 명령을 정확히 남긴다.

## 작업 중 문서화

- 제품 결정이 바뀌면 AI_FESTIVAL_SPEC.md도 수정한다.
- Firebase 컬렉션이나 필드가 바뀌면 데이터 모델 문서를 함께 수정한다.
- 새 환경 변수를 추가하면 .env.example에 빈 값과 설명을 추가한다.
- 리허설에서 발견한 운영 문제는 docs/REHEARSAL.md에 기록한다.

## 하지 말아야 할 것

- 전체 페이지를 한 컴포넌트에 구현
- any 타입 남용
- 학생 화면에 작은 아이콘만 있는 버튼 사용
- 비밀키나 관리자 권한을 클라이언트에 저장
- 사용자 승인 없이 유료 서비스 활성화
- 미션 콘텐츠를 코드 여러 곳에 하드코딩
- 이미지 원본을 불필요하게 여러 번 로드
- 자동 순위를 교사가 수정할 수 없게 고정
- 실제 학생 개인정보를 샘플 데이터에 사용

## 작업 보고 형식

작업 후 다음 네 항목만 간결하게 보고한다.

1. 구현한 내용
2. 변경한 주요 파일
3. 실행한 검사와 결과
4. 다음 한 단계
