# Claude Code 첫 개발 프롬프트

아래 “복사용 프롬프트” 전체를 Claude Code에 붙여 넣는다. 이 프롬프트는 1단계 로컬 목업만 구현하도록 제한되어 있다. Firebase 연결은 화면 동선과 구조를 확인한 다음 별도 단계에서 진행한다.

## 실행 전 준비

1. 이 폴더를 새 Git 저장소의 루트로 사용한다.
2. CLAUDE.md와 AI_FESTIVAL_SPEC.md가 루트에 있는지 확인한다.
3. public/assets/festival과 public/assets/brand의 이미지가 있는지 확인한다.
4. Claude Code를 이 폴더에서 실행한다.

## 복사용 프롬프트

    이 프로젝트는 서울송정초등학교의 송정 AI 페스티벌 웹앱이다.

    작업을 시작하기 전에 CLAUDE.md와 AI_FESTIVAL_SPEC.md를 처음부터 끝까지 읽어라. 두 문서에서 충돌이 있으면 AI_FESTIVAL_SPEC.md를 제품 요구사항의 기준으로 삼고, 무료 운영과 학생 개인정보 보호 원칙을 반드시 지켜라.

    이번 작업에서는 “1단계 로컬 목업”만 구현하라. Firebase 프로젝트를 만들거나 Firebase SDK를 연결하지 말고, Cloud Storage, Cloud Functions, 외부 AI API, 유료 서비스도 추가하지 마라.

    먼저 현재 폴더와 public/assets의 파일을 확인한 뒤, 구현 계획과 예상 파일 구조를 짧게 보여줘. 그 다음 아래 범위를 실제로 구현하고 검사까지 마쳐라.

    기술 조건:

    - React + TypeScript + Vite
    - React Router
    - CSS 변수 기반의 작은 디자인 시스템
    - Vitest와 Testing Library
    - ESLint와 Prettier
    - 일반 아이콘은 Material Symbols Rounded를 사용하되 학교 인터넷에 의존하지 않도록 가능한 자체 포함 방식을 택할 것
    - 큰 UI 프레임워크는 사용하지 말 것
    - package manager는 npm을 사용할 것

    데이터 구조:

    - EventRepository 인터페이스를 만든다.
    - 이번 단계는 MockEventRepository만 구현한다.
    - VITE_DATA_MODE의 기본값은 mock이다.
    - 화면 컴포넌트가 mock 데이터 파일을 직접 import하지 않도록 한다.
    - 샘플 행사는 eventId songjeong-ai-festival-2026을 사용한다.
    - 3학년 4개 반, 4학년 5개 반, 5학년 6개 반, 6학년 5개 반, 학급당 5팀 데이터를 생성한다.

    구현할 공통 요소:

    - 앱 헤더, 학교 로고, 연결 상태 배지, 뒤로 가기, 전체 화면, 소리 토글
    - 학생용 MissionShell
    - 남은 시간 Timer
    - 상태 배지
    - 큰 터치 버튼
    - 로딩, 빈 상태, 오류, 재시도 화면
    - asset manifest
    - 태블릿 가로 1280×800 우선 반응형
    - 세로 화면과 1024×768에서도 기능이 잘리지 않게 할 것
    - prefers-reduced-motion을 존중할 것

    학생 화면:

    1. 시작 화면
       - hero-main.webp를 사용한다.
       - “AI 미션 투어 시작” 버튼
       - “교사용” 링크

    2. 팀 입장 화면
       - 개발용으로 학년, 반, 팀을 고를 수 있다.
       - 실제 QR 진입을 흉내 내는 eventId와 teamId 경로를 지원한다.
       - 선택한 팀을 확인한 뒤 입장한다.

    3. 팀 홈
       - 현재 팀 이름
       - 5개 미션 진행 현황
       - 현재 라운드와 남은 시간
       - 지금 이동할 미션과 교실
       - “미션 시작” 버튼
       - “내 카드 보기” 버튼

    4. 다섯 미션의 목업 화면
       - AI 골든벨: 객관식 한 문제 제출과 정답 공개 상태
       - AI 틀린그림 찾기: 이미지 위의 샘플 원형 영역을 터치해 찾기
       - AI 설명대로 그려라: 실제 HTML Canvas에 펜, 지우개, 색상, 굵기, 실행 취소, 전체 지우기, 제출 미리보기
       - 로봇 길찾기: 규칙, 시작 준비, 교사 확인 대기
       - AI 오류찾기: 잘못된 부분, 올바른 내용, 책 제목, 쪽수 입력과 제출
       - 모든 저장은 mock repository에만 한다.

    5. 카드 뽑기
       - 개발용 미사용 뽑기권 3장을 표시한다.
       - card-back.webp를 눌러 뒤집으면 미리 배정된 카드가 공개된다.
       - 새로고침 전에는 같은 뽑기권을 다시 사용할 수 없게 한다.
       - 카드 획득 시 mascot-card-earned.webp를 사용한다.

    6. 카드함
       - 팀이 가진 5종 카드 수량
       - 학급이 가진 5종 카드 수량
       - 아직 없는 카드 표시
       - 5종 완성 시 scene-finale.webp를 사용한 축하 상태

    교사 화면:

    1. 로그인 목업
       - 실제 Google 로그인은 구현하지 않는다.
       - “개발용 교사로 입장” 버튼만 제공한다.

    2. 운영 대시보드
       - 행사 상태, 활성 학년, 현재 라운드
       - 5개 미션 카드
       - 미션별 제출 팀 수
       - 시작, 일시정지, 종료 버튼은 mock 상태를 변경한다.

    3. 미션 운영 화면
       - 참가 팀 목록
       - 점수와 제출 상태
       - 순위 수정
       - 순위 확정 시 1위 3장, 2위 2장, 나머지 1장으로 뽑기권이 생기는 모습을 mock으로 확인한다.

    4. 학급 카드와 교환 화면
       - 학급별 5종 카드 표
       - 보내는 학급, 받는 학급, 카드, 수량 선택
       - 교환 전후 미리보기
       - 보유량보다 많이 보내면 차단
       - 교환 후 수량 갱신

    이미지 사용:

    - public/assets/festival 안의 WebP를 실제 UI에 연결한다.
    - public/assets/brand/school-logo.png를 헤더에 작게 사용한다.
    - 미션 카드마다 대응하는 mission 이미지가 있어야 한다.
    - 이미지 경로는 asset manifest 한 곳에서 관리한다.
    - 의미 있는 이미지에는 한국어 alt를 넣는다.

    스타일:

    - 분위기는 귀엽고 박진감 있는 AI 스포츠 페스티벌이다.
    - 학교의 초록색을 기본 정체성으로 쓰고 네온 블루, 보라, 주황을 포인트로 쓴다.
    - 본문 18px 이상, 주요 버튼 높이 56px 이상, 터치 영역 48×48px 이상
    - 중요한 행동은 아이콘과 한국어 문구를 함께 표시한다.
    - 한 화면의 대표 행동은 하나만 강하게 강조한다.

    테스트:

    - 팀 번호에 따른 5라운드 미션 순환 함수
    - 순위에 따른 뽑기권 수 계산
    - 카드 5종 완성 판정
    - 보유량을 넘는 교환 차단
    - 사용한 뽑기권 재사용 차단
    - 최소 한 개의 주요 학생 화면 렌더링 테스트

    완료 조건:

    - npm run lint 통과
    - npm run test -- --run 통과
    - npm run build 통과
    - 브라우저에서 학생 입장 → 미션 → 카드 뽑기 → 카드함 흐름이 클릭 가능
    - 교사 대시보드 → 미션 순위 확정 → 카드 교환 흐름이 클릭 가능
    - Firebase 코드와 실제 외부 API 호출이 없음

    구현 도중 요구사항이 불명확하면 학생 개인 정보와 결제 기능을 추가하지 않는 범위에서 가장 단순한 기본값을 선택하고, 마지막 보고에 그 가정을 적어라. 완료 후에는 구현 내용, 주요 파일, 검사 결과, 다음 한 단계만 간결하게 보고하라.

## 1단계가 끝난 뒤 확인할 것

- 학생 화면이 글보다 이미지와 행동 중심인지
- 다섯 미션의 차이가 즉시 보이는지
- 한 학년의 4~6팀이 한 미션 화면에서 자연스럽게 비교되는지
- 교사가 현재 라운드와 미제출 팀을 빠르게 찾을 수 있는지
- 카드 뽑기와 교환이 학생에게 충분히 재미있어 보이는지
- 디벗 실제 화면 크기에서 버튼이 작지 않은지

이 확인이 끝난 뒤에만 Firebase 연동 프롬프트를 작성한다.
