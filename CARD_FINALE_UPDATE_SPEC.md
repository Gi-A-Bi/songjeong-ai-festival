# 송정 AI 페스티벌 실시간 운영·카드·최종 미션 변경 명세

문서 버전: 3.0  
작성 기준일: 2026-09-17  
적용 범위: 실시간 운영 대시보드, 팀 이동 추적, 카드 성장, 학급 전체 최종 미션, 최종 순위

이 문서는 `AI_FESTIVAL_SPEC.md`의 카드 찬스, AI 마스터 찬스, 팀별 결승 문제, 최종 코드, 학년 동시 출발, 결승 순위 관련 요구사항을 대체한다. 두 문서가 충돌하면 이 문서를 우선한다.

## 1. 최신 결정 요약

1. 한 학년의 모든 팀이 5개의 미션 교실을 5라운드 동안 순환한다.
2. 팀은 각 미션 교실의 QR을 찍어 도착을 기록한다.
3. 부스 교사는 라운드를 시작하고 종료 후 순위와 결과를 확정한다.
4. 순위에 따라 각 팀은 카드 종류를 선택하거나 자동 배정받는다.
5. 카드 조각은 학급 단위로 합산하며 같은 종류 4조각이면 카드가 완성된다.
6. 총괄 대시보드는 어떤 학급의 어떤 팀이 어느 미션에 있는지 실시간으로 보여준다.
7. 투어가 끝나면 총괄 운영자가 해당 학년의 최종 미션을 연다.
8. 각 반은 준비되었을 때 담임교사 대시보드에서 최종 미션을 시작한다.
9. 학급 전체가 전자칠판을 보며 10개의 객관식 문제를 함께 푼다.
10. 완성한 카드 종류 수만큼 공통 힌트를 사용할 수 있다.
11. 최종 순위는 `정답 수 → 5종 카드 완성 여부 → 실제 소요 시간` 순으로 정한다.

## 2. 폐기되는 이전 요구사항

- 순위별 3장·2장·1장 카드 뽑기권
- 카드 뒤집기와 무작위 카드 뽑기 화면
- 학급 간 카드 교환과 조각 재조합
- 팀별로 서로 다른 결승 문제를 푸는 구조
- 다섯 팀의 답을 조합해 최종 코드를 만드는 구조
- 카드 종류마다 서로 다른 효과를 주는 찬스
- 대응 카드의 담당 팀에서만 찬스를 사용하는 규칙
- AI 마스터 찬스와 틀린 팀 번호 확인 기능
- 결승 오답 코드 제출 후 30초 잠금
- 같은 학년 모든 반의 강제 동시 시작
- 첫 정답 코드 제출 시각만으로 우승을 정하는 규칙

`drawTickets`, `exchanges`, `FinalTeamAnswer`, `masterChanceAvailable`, `masterChanceUsed`, `finalCode`, `wrongCodeAttempts`, `retryLockedUntil` 등 폐기 기능만을 위한 모델과 코드는 제거하거나 마이그레이션한다. 기존 `/draw`, `/exchange`, 팀별 `/final` 경로는 새 카드 현황 또는 학급 최종 미션 안내 화면으로 리다이렉트할 수 있다.

## 3. 역할과 권한

| 역할 | 열람 범위 | 수정 권한 |
| --- | --- | --- |
| 총괄 운영자 `admin` | 모든 학년·학급·미션·카드·최종 결과 | 라운드 제어, 최종 미션 열기, 오류 보정, 결과 공개 |
| 부스 교사 `station_teacher` | 전체 현황 읽기, 담당 미션 상세 | 담당 미션 시작, 결과·순위 확정, 재입장 허용 |
| 담임교사 `homeroom_teacher` | 전체 현황 읽기, 담당 학급 상세 | 담당 학급 최종 미션 시작·진행·제출 |

읽기 권한과 수정 권한을 분리한다. 다른 부스나 다른 학급의 결과를 임의로 수정할 수 없어야 한다. 총괄 운영자는 네트워크·입력 오류를 복구할 수 있어야 하며, 수동 수정 시 사유와 수정자를 기록한다.

## 4. 실시간 운영 대시보드

대시보드는 행사 전 과정을 보여주는 운영 상황판이다. 기본 화면은 현재 학년과 현재 라운드만 실시간 구독한다.

### 4.1 상단 요약

- 현재 학년과 현재 라운드 `n/5`
- 라운드 상태: 준비, 활동 중, 이동 중, 종료
- 활동 또는 이동 남은 시간
- 입장 완료 팀 수 / 예정 팀 수
- 완료 팀 수 / 예정 팀 수
- 확인이 필요한 경고 수

### 4.2 미션 교실별 현황

5개 미션 교실을 카드 또는 열로 표시한다. 각 미션 카드에는 미션명, 교실명, 담당 교사, 현재 상태, 예정된 학급·팀 배지, 팀별 상태, 결과 입력 여부를 표시한다.

### 4.3 학급·팀별 현황

학급 행과 1~5팀 열을 가진 표를 제공한다. 각 셀에는 현재 미션과 상태를 표시한다. 학급 상세에는 다음 정보가 있어야 한다.

- 팀별 현재 미션과 다음 미션
- 완료한 미션 수와 미션별 순위
- 획득한 카드 종류
- 카드 5종 진행도
- 완성 카드 종류 수와 최종 힌트 예상 수

### 4.4 실시간 활동 기록

다음과 같은 중요한 상태 변경만 최근 활동으로 기록한다.

- 미션 교실 입장
- 미션 시작과 완료
- 순위·결과 확정
- 카드 조각 획득과 카드 완성
- 라운드 상태 변경
- 미도착, 잘못된 교실, 결과 미입력 경고
- 최종 미션 시작과 제출

화면 조회나 타이머 1초 변화처럼 의미 없는 이벤트는 저장하지 않는다.

### 4.5 상태 표현

- 회색: 입장 전 또는 준비
- 파랑: 입장 완료
- 주황: 진행 중 또는 결과 입력 중
- 초록: 완료
- 빨강: 확인 필요

색상만으로 상태를 전달하지 않는다. 항상 아이콘과 한국어 상태명을 함께 표시한다.

## 5. 팀 이동과 QR 체크인

### 5.1 기본 방식

- 팀은 최초 팀 QR로 자기 팀 세션에 입장한다.
- 각 미션 교실에는 해당 미션을 식별하는 QR을 둔다.
- 팀 기기에서 미션 QR을 찍으면 `eventId`, `grade`, `round`, `teamId`, `stationId`를 검증한 뒤 체크인한다.
- 같은 팀이 같은 라운드와 같은 미션에 여러 번 체크인해도 기록은 하나만 생성한다.

### 5.2 예상 미션 검증

```text
expectedMissionNo = ((teamNo - 1 + roundNo - 1) mod 5) + 1
```

스캔한 미션이 예상 미션과 다르면 정상 완료 처리하지 않고 `wrong_station` 경고를 표시한다. 팀 화면에는 가야 할 교실을 안내하고 대시보드에는 총괄 확인 항목으로 표시한다.

### 5.3 상태 흐름과 경고

```text
scheduled → checked_in → active → completed → moving
```

예외는 `attention`으로 표현하고 다음 원인 코드를 별도 저장한다.

- `not_arrived`
- `wrong_station`
- `result_missing`
- `duplicate_scan`
- `manual_review`

동일 QR 재스캔은 오류로 겁주지 말고 이미 입장했다는 안내만 보여준다. 라운드 시작 후 기본 2분이 지나도 체크인하지 않으면 `not_arrived`, 라운드 종료 후 결과가 확정되지 않으면 `result_missing`을 표시한다.

## 6. 부스 교사 흐름

부스 교사의 조작은 다음 네 단계로 제한한다.

1. 예정 팀과 체크인 여부 확인
2. `미션 시작`
3. 미션 종료 후 결과 또는 순위 입력
4. `결과 확정`

미션 시작 시 체크인한 팀을 `active`로 바꾸고, 결과 확정 시 해당 팀을 `completed`로 바꾼다. 결과 확정은 고정 문서 ID 또는 `requestId`를 사용해 중복 클릭으로 카드 보상이 두 번 생성되지 않게 한다.

부스 화면은 담당 미션·현재 학년·보고 있는 라운드의 제출만 실시간으로 구독해(문서 수는 학급 수만큼) 학생 제출을 새로고침 없이 보여 준다. 목록은 구독 캐시로 만들어 제출을 되풀이해 읽지 않는다.

- 새 제출이 들어와 목록을 다시 그려도 교사가 입력하던 점수와 직접 바꾼 순위는 지우지 않는다.
- 결과 확정 직전에 서버에서 제출을 한 번 더 읽어, 화면이 받지 못한 제출이 있으면 확정하지 않고 목록을 다시 불러와 알린다.
- 보고 있는 라운드의 결과를 이미 확정했다면 다음 라운드가 열릴 때(이동 시간 포함) 화면이 그 라운드로 넘어간다. 확정 전이거나 교사가 지난 라운드를 직접 골랐다면 옮기지 않고 “지금 팀이 들어오는 라운드” 안내와 이동 버튼만 보여 준다.

## 7. 카드 보상과 네 조각 성장

### 7.1 카드 종류

- 생각 `thinking`
- 관찰 `observation`
- 표현 `expression`
- 명령 `command`
- 검증 `verification`

### 7.2 순위별 선택권

모든 팀은 미션 하나를 마칠 때 카드 조각을 정확히 하나 받는다. 순위는 수량이 아니라 종류 선택권에만 영향을 준다.

| 순위 | 카드 종류 결정 방식 |
| --- | --- |
| 1위 | 서로 다른 3종 중 1종 선택 |
| 2위 | 서로 다른 2종 중 1종 선택 |
| 3위 이하 | 1종 무작위 자동 배정 |

- 후보는 5종에서 같은 확률로 만들고 한 보상 안에서 중복하지 않는다.
- 1·2위 팀은 선택 전에 학급 카드 진행도를 볼 수 있다.
- `offeredTypes`에 없는 값을 선택할 수 없다.
- 자동 배정은 생성과 동시에 선택 완료 상태로 저장한다.
- 보상 생성과 수령은 멱등성을 보장한다.

### 7.3 학급 단위 카드 성장

- 1회: 왼쪽 위 공개
- 2회: 오른쪽 위 공개
- 3회: 왼쪽 아래 공개
- 4회: 오른쪽 아래 공개 및 완성
- 5회 이후: `4/4`를 유지하고 중복 `+N`만 증가

중복 조각은 교환하거나 다른 카드로 바꾸지 않는다. 기존 `card-*.webp` 한 장을 CSS 마스크 또는 오버레이로 네 영역에 나누어 공개한다.

### 7.4 최종 미션과 카드의 관계

- 완성 카드의 종류 수가 학급의 힌트 수가 된다.
- 카드 종류에 따라 힌트 효과가 달라지지 않는다.
- 중복 조각은 힌트를 늘리지 않는다.
- 최대 힌트는 5개다.
- 5종 모두 완성했는지는 최종 순위의 두 번째 판정 기준이다.
- AI 마스터 찬스는 존재하지 않는다.

## 8. 최종 미션 개방과 학급별 시작

### 8.1 개방 조건

총괄 운영자는 5라운드 종료, 모든 팀의 결과 입력, 미처리 카드 선택 완료를 확인한 뒤 학년 최종 미션을 연다. 운영상 강제로 열 때는 확인 대화상자와 사유 기록이 필요하다.

### 8.2 학급별 시작

- 최종 미션이 열린 뒤 각 반 담임교사 화면에 `최종 미션 시작` 버튼을 활성화한다.
- 반마다 실제 시작 시각은 달라도 된다.
- 시작 전 `시작하면 시간이 측정되며 다시 시작할 수 없습니다.`라는 확인창을 표시한다.
- 확인 뒤 `3, 2, 1, 시작!` 카운트다운을 보여준다.
- 카운트다운이 끝나는 시점에 repository를 통해 시작을 한 번만 기록한다.
- 새로고침하거나 다른 기기에서 다시 열어도 기존 시작 시각을 유지하고 진행을 복구한다.
- 관리자만 오류 상황에서 세션을 초기화하거나 시간을 보정할 수 있다.

시작 시 다음 값을 스냅샷으로 고정한다.

- `completedCardTypeCountSnapshot`
- `allFiveCardsCompletedSnapshot`
- `hintTotal`

시작 후 카드 데이터가 수정되어도 진행 중 세션의 힌트 수와 동점 판정 값은 자동으로 바뀌지 않는다. 관리자가 재시작을 승인한 경우에만 새 스냅샷을 만든다.

## 9. 학급 전체 최종 미션

### 9.1 기본 구조

- 학급 전체가 전자칠판 한 화면을 보며 함께 푼다.
- 총 10문제이며 모두 4지선다형이다.
- 기본 제한 시간은 12분이며 행사 설정에서 변경할 수 있다.
- 같은 학년의 모든 학급에는 동일한 문제와 제한 시간을 사용한다.
- 문제와 보기, 정답, 오답 제거 대상은 컴포넌트에 하드코딩하지 않고 repository 또는 config에서 불러온다.

권장 구성은 생각·관찰·표현·명령·검증 영역을 2문제씩 배치하는 것이다.

### 9.2 문제 풀이 화면

- 현재 문제 번호 `n/10`
- 전체 경과 시간 또는 남은 시간
- 남은 힌트 수
- 문제와 이미지 또는 자료
- 보기 4개
- `힌트 사용`
- `답 확정하고 다음 문제로`

답 확정 전에는 변경할 수 있고, 확정 뒤에는 일반 교사 계정이 되돌리지 못한다. 네트워크 오류와 새로고침 후에도 확정한 답과 현재 문제를 복구한다. 중간에는 정답 여부를 보여주지 않는다.

마지막 문제까지 확정하면 전체 답안을 제출한다. 제한 시간이 끝나면 저장된 답안을 제출하고 미응답 문제는 오답 처리한다. 브라우저가 닫혀 자동 제출되지 못한 경우 대시보드에 `시간 초과·제출 확인 필요`를 표시하고 총괄 운영자가 강제 마감할 수 있게 한다.

### 9.3 공통 힌트

- 힌트 1개를 사용하면 현재 문제의 틀린 보기 1개를 제거한다.
- 어느 카드 종류를 완성했는지는 힌트 효과에 영향을 주지 않는다.
- 한 문제에는 힌트를 최대 1개만 사용할 수 있다.
- 힌트 사용은 확정 전 한 번만 가능하다.
- 이미 선택한 보기가 제거 대상이면 제거 전에 선택을 해제한다.
- 정답 보기는 절대 제거하지 않는다.
- 사용 기록은 저장하여 새로고침 후에도 복구하고 두 번 차감되지 않게 한다.

## 10. 시간 측정과 최종 순위

### 10.1 시간 측정

```text
durationMs = submittedAt - startedAt
```

- `startedAt`과 `submittedAt`은 repository의 서버 시각을 사용한다.
- 화면의 1초 타이머는 저장된 시작 시각으로 클라이언트에서 계산한다.
- 매초 데이터베이스에 시간을 쓰지 않는다.
- 시작과 제출은 고정 ID 또는 `requestId`로 중복 기록을 막는다.

### 10.2 점수와 순위

`correctCount`는 정답인 응답 수이며 최대 10점이다. 다음 순서로 비교한다.

1. `correctCount`가 많은 학급
2. 정답 수가 같으면 `allFiveCardsCompletedSnapshot`이 true인 학급
3. 카드 완성 여부도 같으면 `durationMs`가 짧은 학급
4. 세 값이 모두 같으면 공동 순위

```text
correctCount DESC
→ allFiveCardsCompletedSnapshot DESC
→ durationMs ASC
```

5종을 완성하지 못한 학급끼리 동점인 경우에도 소요 시간으로 순위를 정한다. 완성 카드가 3종인지 4종인지는 추가 동점 기준으로 사용하지 않는다.

### 10.3 결과 공개

- 진행 중에는 상태, 현재 문제 번호, 경과 시간, 남은 힌트만 중계한다.
- 일반 교사·학급 화면에는 같은 학년의 모든 반이 제출하기 전까지 정답 수와 순위를 숨긴다.
- 모든 반 제출 후 총괄 운영자가 결과를 공개한다.
- 결과에는 정답 수, 5종 완성 여부, 소요 시간, 최종 순위를 표시한다.

## 11. 권장 화면과 경로

| 경로 | 화면 |
| --- | --- |
| `/team/:eventId/:teamId` | 팀 홈, 현재·다음 미션 안내 |
| `/team/:eventId/:teamId/check-in/:stationId` | 미션 QR 체크인 결과 |
| `/team/:eventId/:teamId/reward` | 카드 후보 선택 또는 자동 배정 확인 |
| `/team/:eventId/:teamId/cards` | 학급 카드 5종 진행도 |
| `/teacher/:eventId/dashboard` | 실시간 전체 운영 대시보드 |
| `/teacher/:eventId/station/:stationId` | 부스 교사 운영·결과 입력 |
| `/teacher/:eventId/class/:classId` | 담임용 학급 상세와 최종 미션 시작 |
| `/teacher/:eventId/class/:classId/final` | 전자칠판용 10문제 최종 미션 |
| `/teacher/:eventId/final-results` | 학년별 최종 결과와 순위 |

팀별 결승 문제 경로는 만들지 않는다.

## 12. 핵심 데이터 모델

화면 컴포넌트는 Firestore SDK를 직접 호출하지 않는다. `EventRepository` 인터페이스를 통해 읽고 쓴다. 먼저 `MockEventRepository`를 구현하고 Firebase 단계에서는 동일 인터페이스를 유지한다.

```ts
type TeamMissionStatus =
  | "scheduled"
  | "checked_in"
  | "active"
  | "completed"
  | "moving"
  | "attention";

interface TeamMissionState {
  id: string; // `${classId}_${teamNo}_${roundNo}`
  eventId: string;
  grade: number;
  classId: string;
  teamNo: 1 | 2 | 3 | 4 | 5;
  roundNo: 1 | 2 | 3 | 4 | 5;
  expectedMissionId: string;
  actualMissionId: string | null;
  status: TeamMissionStatus;
  alertCodes: string[];
  checkedInAt: Date | null;
  startedAt: Date | null;
  completedAt: Date | null;
  resultId: string | null;
  updatedAt: Date;
}

interface MissionRoundState {
  id: string; // `${missionId}_${roundNo}`
  eventId: string;
  grade: number;
  missionId: string;
  roundNo: number;
  status: "ready" | "active" | "scoring" | "completed";
  startedAt: Date | null;
  completedAt: Date | null;
  resultFinalizedAt: Date | null;
  updatedBy: string | null;
}

interface FinalSession {
  id: string; // grade
  eventId: string;
  grade: number;
  status: "locked" | "open" | "results_hidden" | "results_published" | "closed";
  questionCount: 10;
  durationLimitSec: number;
  openedAt: Date | null;
  openedBy: string | null;
  resultsPublishedAt: Date | null;
}

interface FinalClassState {
  id: string; // classId
  eventId: string;
  grade: number;
  classId: string;
  status: "locked" | "ready" | "active" | "submitted" | "timeout" | "review_required";
  currentQuestionIndex: number;
  completedCardTypeCountSnapshot: number;
  allFiveCardsCompletedSnapshot: boolean;
  hintTotal: number;
  hintUsed: number;
  startedAt: Date | null;
  submittedAt: Date | null;
  durationMs: number | null;
  correctCount: number | null;
  finalRank: number | null;
  manualOverride: boolean;
  overrideReason: string | null;
  updatedAt: Date;
}

interface FinalResponse {
  id: string; // `${classId}_${questionId}`
  eventId: string;
  grade: number;
  classId: string;
  questionId: string;
  selectedChoiceId: string | null;
  hintUsed: boolean;
  removedChoiceId: string | null;
  confirmedAt: Date | null;
  updatedAt: Date;
}
```

선택 완료된 `cardAwards`가 카드 진행도의 원본 원장이다. 카드별 진행도는 `min(4, 획득 수)`, 중복은 `max(0, 획득 수 - 4)`, 완성 카드 종류 수는 진행도 4인 종류의 개수다.

중요한 상태 변경은 `activityEvents`에 저장하되 동일한 원인에서 중복 생성되지 않도록 결정적인 ID를 사용한다.

### 구현 메모

- 부스 상태 문서 ID는 학년이 섞이지 않도록 `${missionId}_g${grade}_r${roundNo}`를 쓴다. 팀 상태 문서 ID는 `${classId}_${teamNo}_${roundNo}`이다.
- 팀의 화면 표시 상태와 시간 기반 경고(`not_arrived`, `result_missing`)는 저장하지 않고 읽을 때 라운드 시각으로 계산한다. 저장하는 경고는 `wrong_station`뿐이다. 일시정지한 시간은 미도착 계산에서 뺀다.
- `result_missing`은 라운드 종료 후 이동 시간(기본 2분)이 지나도 결과가 없거나, 다음 라운드가 이미 시작된 경우에 표시한다. 채점 중인 부스가 곧바로 경고로 뜨지 않게 하기 위해서다.
- QR 체크인 대상 라운드는 활동 중에는 현재 라운드, 이동 시간에는 다음 라운드다.
- 팀의 완료 여부는 `teamMissionStates`가 아니라 `results`를 원본으로 삼는다. 결과 확정·수정이 팀 상태와 어긋나면 결과를 따른다.
- 학급 최종 상태 중 `locked`(학년 미개방)와 `review_required`(제한 시간 + 20초가 지나도 제출 기록 없음)는 저장하지 않고 읽을 때 계산한다.
- 제한 시간은 어느 반도 시작하기 전에만 바꿀 수 있다. 시작 뒤에 바꾸면 반마다 조건이 달라지기 때문이다.
- 강제 마감은 저장된 답안으로 채점하며, 시작하지 못한 반은 0점·제한 시간 전체로 기록한다.
- 관리자가 직접 정한 순위(`manualOverride` + `finalRank`)는 자동 순위 계산이 덮어쓰지 않는다.
- QR 체크인·대시보드·최종 미션은 mock과 Firestore 저장소 모두에서 동작한다. Firestore 쪽은 보안 규칙 테스트와 저장소 에뮬레이터 테스트(`npm run test:emulator`)로 확인한다.

Firestore 구현에서 12장의 모델과 다르게 한 점(무료 사용량과 보안 때문):

- `teamMissionStates`에는 체크인 기록만 저장한다. 진행 중은 부스 문서의 시작 시각, 완료는 부스 문서의 `resultTeamIds`(순위를 확정한 팀)로 계산한다. 부스가 미션을 시작할 때 팀 문서를 다시 쓰지 않는다.
- `activityEvents` 컬렉션은 만들지 않는다. 최근 활동은 구독 중인 문서의 시각으로 만든다.
- `finalResponses`는 문제별 문서가 아니라 학급당 문서 하나다(`answers["0"~"9"]`). 보안 규칙은 지금 푸는 번호만 바꿀 수 있게 한다.
- 정답은 `finalAnswerKeys/{학년}`에 따로 두고 총괄 운영자만 읽는다. 담임교사 기기는 채점하지 않으며, 점수·소요 시간·순위는 총괄 운영자가 결과를 공개할 때 학급 상태에 기록한다. 공개 전 총괄 화면의 점수와 순위는 총괄 기기에서 계산한 미리 보기다.
- 세션의 `results_hidden`, 학급의 `locked`·`ready`·`review_required`는 저장하지 않고 계산한다. 시작 전인 학급은 상태 문서가 없다.
- 시작·제출·입장 시각은 서버 시각(`serverTimestamp`)이며 보안 규칙이 `request.time`과 같은지 검사한다. 제출은 제한 시간 + 30초 안에서만, 시간 마감은 제한 시간이 지난 뒤에만 기록된다.
- 같은 요청이 거의 동시에 두 번 들어오면(연타, 같은 팀의 두 기기) 늦은 쪽은 보안 규칙에서 거부될 수 있다. 저장소는 이를 오류로 보지 않고 이미 처리됐는지 다시 확인해 같은 결과를 돌려준다.

## 13. 실시간 처리와 무료 사용량 보호

- 현재 학년과 현재 라운드의 작은 상태 컬렉션만 실시간 구독한다.
- 타이머는 매초 Firestore에 저장하지 않는다.
- QR 중복 스캔은 같은 문서 ID에 멱등 처리한다.
- 카드 보상과 힌트 사용은 트랜잭션 또는 원자적 저장으로 중복을 막는다.
- 큰 이미지 제출 문서를 실시간 대시보드 쿼리에 포함하지 않는다.
- 활동 기록은 중요한 상태 변경에만 생성한다.
- Cloud Functions, Cloud Storage, Firebase AI Logic, 외부 생성형 AI API를 추가하지 않는다.
- Firebase Spark 플랜에서 동작하는 구조를 유지한다.

## 14. 오류와 복구

- 새로고침 후에도 팀 위치, 미션 상태, 카드 선택, 최종 문제 진행, 힌트 사용을 복구한다.
- 제출 버튼 연타로 결과가 중복 저장되지 않게 한다.
- 연결이 끊기면 현재 입력을 로컬에 임시 보존하고 재연결 후 같은 요청 ID로 재시도한다.
- 총괄 운영자는 잘못된 체크인 취소, 팀 재배치, 결과 재입력, 최종 시간·점수 보정을 할 수 있다.
- 수동 보정에는 수정자, 시각, 사유를 기록한다.
- 위험한 초기화와 결과 공개에는 확인 대화상자를 사용한다.

## 15. 필수 테스트

### 미션 투어와 대시보드

- 예상 미션 계산
- 정상·오입장·중복 QR 체크인
- 부스 시작과 결과 확정 상태 전환
- 결과 확정 재시도 시 카드 보상 중복 방지
- 현재 학년·라운드의 대시보드 요약과 경고 계산
- 중요한 상태 변경만 활동 기록에 표시

### 카드

- 순위별 후보 개수 3, 2, 1
- 후보 카드 종류 중복 방지와 허용 범위 밖 선택 차단
- 카드 진행도 0/4~4/4와 중복 +N
- 완성 카드 종류 수와 힌트 수 일치
- 중복 조각이 힌트 수를 늘리지 않음
- 5종 완성 여부 판정

### 최종 미션

- 개방 전 시작 버튼 비활성화
- 학급별 시작 멱등성과 새로고침 복구
- 시작 시 카드와 힌트 수 스냅샷 고정
- 10문제 순차 진행과 답 복구
- 힌트가 정답이 아닌 보기 하나만 제거
- 같은 문제 힌트 재사용과 보유량 초과 차단
- 제한 시간 종료 시 저장 답안 마감과 미응답 오답 처리
- 서버 시각 기반 소요 시간 계산
- 정답 수 내림차순, 5종 완성 우선, 소요 시간 오름차순 순위
- 5종 미완성 학급끼리도 시간으로 동점 해결
- 모든 학급 제출 전 일반 화면에서 순위 숨김
- 관리자 수동 보정 사유 검증

## 16. 완료 조건

1. 총괄 대시보드에서 현재 학년의 모든 팀 위치와 상태를 확인할 수 있다.
2. 미도착, 잘못된 교실, 결과 미입력 상황을 한눈에 찾을 수 있다.
3. QR 재스캔과 결과 확정 재시도로 데이터가 중복되지 않는다.
4. 학급별 카드 진행도와 완성 카드 종류 수가 정확하다.
5. 카드 종류와 무관하게 완성 종류 수만큼 공통 힌트를 사용할 수 있다.
6. 학급 전체가 전자칠판 한 화면에서 10문제를 풀 수 있다.
7. 반마다 시작 시각이 달라도 실제 소요 시간이 정확히 계산된다.
8. 최종 순위가 `정답 수 → 5종 완성 여부 → 소요 시간` 규칙과 일치한다.
9. 진행 중에는 다른 반의 점수와 순위가 노출되지 않는다.
10. 새로고침과 일시적 네트워크 장애 뒤에도 핵심 상태를 복구한다.
11. 학생 개인정보, 유료 API, Cloud Functions, Cloud Storage를 추가하지 않는다.
